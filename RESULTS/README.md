# RESULTS
The 2 tasks were created, and they both work, and the context switching is also displayed.
For now the second task prints "ACTUATING NOW" when the current is more than 0.1A (Message prints when servo is moving). The servo is controlled by a external servo tester for now, but the final version will be controlled directly with the STM32 Blue Pill MCU

Tasks being run and switched every 50ms:
